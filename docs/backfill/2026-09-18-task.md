# AI4S Monitor 一次性全量分析任务

启动时间：2026-09-18 08:00（Asia/Shanghai）。

这是定时执行规范；保存本文件不代表分析已经执行或完成。固定初始清单见 [2026-09-18-manifest.json](./2026-09-18-manifest.json)。

## 完整任务提示词

在北京时间 2026 年 9 月 18 日 08:00，执行一次 AI4S Monitor 全量积压分析与 Pages 发布任务。仓库：https://github.com/chuanyue20031107/ai4s-monitor，分支 main。用户明确要求把当前所有未分析情报逐条分析并转成已分析状态；这不是每日增量任务，也不是只提醒用户运行。

范围与结束条件：
1. 先读取 AGENTS.md、docs/chatgpt-analysis.md、docs/backfill/2026-09-18-manifest.json、data/queue.json、data/ai-commands/queue.json（如存在）、data/receipts.json 和近期 inbox。清单创建时共 723 条，250 条 ready、473 条缺正文。以清单中的全部 ID 与首次启动时当前 queue 全部 ID 的并集为本次目标，持久化冻结后的清单、起始数量和进度；后续恢复只使用该清单。执行期间新增且不在目标内的记录单独统计，不无限扩展本次任务。
2. 必须遍历目标全集，不得按日期、来源、分数筛掉，不得只取前 30/50/100/500 条。原协议“每次最多 30 条”的日常增量配额不适用于这次用户明确要求的全量任务；仍可每批 20–30 条、每个 inbox 至多 50 条，提交后接着处理下一批，直至全部目标已完成或真实阻塞已穷尽可行修复。AI_BATCH_SIZE=all 只挑 ready，不能作为全量完成的证明。
3. 在本线程工作区 work/ai4s-full-backfill-20260918/ 持久化 manifest、checkpoint、批次文件名、commit SHA、回执、错误与剩余 ID。上下文压缩、批次边界、工具单次返回上限、需要等待部署均不是任务完成理由。持续推进同一任务，恢复时先查远端去重。禁止偷偷另建每日任务或永久轮询；若运行被环境硬性中断，保存断点并明确报告未完成。

先解决证据，再分析：
4. 逐条读取完整 data/raw/<id>.json。ready 也须检查是否为有效正文，不能把导航、标题或营销菜单当实验依据。对 needs_fetch、insufficient_content、fetch_failed 逐项补抓公开原文，复用现有采集器、来源修复和历史补抓能力；不能只抓首页的新文章。现有采集器每来源限量且优先新链接，反复运行时要检查旧 ID 是否真正被覆盖，避免一直重试最前面的失败项而饿死后面的积压。
5. 本次是用户授权继续修改仓库的全量任务。若现有工具不能覆盖历史 ID，作为仓库维护工作实现最小的按目标 ID/原 URL 补抓与断点恢复修复，保留采集限流、访问边界和哈希计算；按仓库要求完成 node --test tests/pipeline.test.mjs、相关新增测试、npm run typecheck、npm run build 后发布必要变更。正文必须由真实采集得到并通过采集流程落入 raw；不要通过改 contentStatus 或伪造 contentHash 绕过证据要求，不添加收费模型 API、外部数据库或前端秘密。网络临时错误按现有退避机制重试，适配可公开访问的原站页面/官方摘要；不绕过登录、验证码、付费墙或 robots。
6. 采集和分析分阶段提交：正文更新入库后重新读取最新 raw.contentHash，再生成分析。不要在同一批次分析的同时改其 raw，避免整个 inbox 因过期哈希被拒。不覆盖其他人或其他任务的并发结果，不 force-push。

逐条分析与发布：
7. 对每个有充分证据的目标生成真正的中文 AI4S 分析，遵循协议字段和字典：category、summary（40–2500 字符）、score（1–5）及 importanceReason、contentType、moatTags、1–5 个 evidence、limitations、analyzedAt。区分原文事实、来源自述与分析判断；保留可追溯 URL。证据摘录必须逐字存在于当前 raw.content。不得批量复制模板、仅复述标题或捏造结果。对确实无关的记录按协议 discarded 并写原因，对证据仍缺失的记录保留真实失败/待处理状态；这两类均不得计作用户要求的 done。
8. 分析阶段只新增唯一 data/inbox/<UTC时间戳>-<唯一后缀>.json，schemaVersion=1、generator=chatgpt-task。提交 main，提交信息不含 [skip ci]。分析阶段不要直接改 queue、receipts、任务 results 或前端派生数据的完成状态。由既有 publisher 校验并导出。
9. 使用本地验证或仓库发布器检查 JSON、字段、原文摘录及哈希，拒绝批次按具体原因修复并另建新批次。检查每批远端提交与 data/receipts.json；accepted 才算落库。检查 Crawl and deploy 工作流 deploy 作业成功，不能把 verify/build 成功或 deploy skipped 当作发布完成。合并并发更新时重新核对所有目标的当前哈希，补做失效分析。

严格验收：
10. 对冻结目标逐 ID 核对最新 raw、有效 accepted inbox、client/public/data/articles.json、analysis-status.json 以及线上 https://chuanyue20031107.github.io/ai4s-monitor/ 实际加载的数据，确认摘要、评分、来源链接和 AI已分析 状态可用。检查线上文章详情与“AI已分析”列表；不要只看仓库 JSON 或首页 HTTP 200。
11. 仅当目标总数=当前有效 done 数、目标中非 done 数=0、目标缺正文数=0、所有有效批次 accepted 且线上 Pages 验证成功，才宣布“当前全部未分析情报已分析并展示”。若不存在执行期间新增数据，全局 pending 也应为 0；若新增数据使其非零，必须列明新增数与本次范围，不能偷换统计口径。discarded 不等于 done，failed 也不等于 done。
12. 完成后给出起始总数、成功 done 数、discarded 数、failed 数、缺正文数、剩余目标 ID、执行期间新增未分析数、提交链接、Actions 运行链接与 Pages 链接。如遇不可恢复的访问/权限/内容阻塞，先完成所有可处理项，再明确报告“全量目标尚未完成”、具体受阻记录与恢复步骤，不把任务创建成功、已入队、已提交或已处理一部分说成全量完成。正常等待和无实质变化时保持安静；仅在完成、重大失败或确需用户操作时通知。此任务只启动一次，不创建持续增量计划。
