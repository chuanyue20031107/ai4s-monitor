/**
 * Retired compatibility surface.
 * AI analysis is performed by Codex in the workspace; the publisher must not
 * admit, reserve, or mutate GitHub analysis commands.
 */
export function reconcileCommands() {
  return { reserved: new Set(), results: [], tasks: [] };
}
